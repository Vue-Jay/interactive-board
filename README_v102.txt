OnlineRepetitor v102
- Панель инструментов опущена ниже верхней панели.
- Её высота теперь ограничена видимой областью окна.
- Длинный список прокручивается внутри панели, а не уезжает за экран.
- Выбрать (стрелка) и Двигать (рука) вынесены в постоянный верхний блок.
- Эти две кнопки всегда остаются на виду независимо от прокрутки и «Ещё инструменты».
SQL не нужен. TypeScript: OK.

npm run build
git add .
git commit -m "v102: fix toolbar layout and pin navigation tools"
git push origin main
