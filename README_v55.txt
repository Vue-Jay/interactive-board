OnlineRepetitor v55 — координатная плоскость и графики

Добавлено:
- новый инструмент «График функции» (Y / Н);
- линейная функция, парабола, синус и косинус;
- коэффициенты a, b, c;
- диапазоны X/Y;
- сетка и предпросмотр;
- редактирование графика двойным щелчком;
- график сохраняется как объект доски;
- исправлена видимая вкладка «Геометрия» редактора формул v54.

SQL не требуется.

node install_v55.mjs
npm run build
git add .
git commit -m "v55: add function graphs"
git push
