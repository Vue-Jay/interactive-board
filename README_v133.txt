OnlineRepetitor v133 - диагностика WebRTC/TURN

Сделано:
- клиент теперь явно определяет, получил ли он TURN-конфигурацию;
- в окне активного звонка показывается режим "TURN" или "Прямое соединение";
- диагностический статус не раскрывает адреса, логины или credentials;
- состояние очищается при завершении звонка;
- это упрощает межсетевой тест после развёртывания coturn.

Важно:
Надпись "TURN" означает, что TURN-сервер присутствует в ICE-конфигурации клиента. Она не доказывает, что выбранная ICE candidate pair действительно relay. Проверку фактического relay добавим отдельно после реального развёртывания TURN.

Следующий этап: развернуть coturn + Edge Function и проверить звонок Wi-Fi <-> мобильная сеть.
