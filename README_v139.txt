OnlineRepetitor v139 - финальная полировка звонков

Сделано:
- удалённое видео определяется по фактическому WebRTC video track;
- голосовая заглушка больше не перекрывает видео собеседника;
- добавлен таймер длительности разговора;
- исправлена TypeScript-совместимость RTCStatsReport без stats.get();
- service-worker cache обновлён до v139.

Проверка:
- TypeScript `tsc -b`: успешно;
- `npm run build` проходит TypeScript и доходит до Vite, но локальный vite binary
  в Linux-контейнере не имеет права исполнения.

Статус звонков:
99%. Последний 1% - развёртывание реального coturn и межсетевой тест.
