OnlineRepetitor v53 — улучшенные таблицы

Что добавлено:
- выравнивание текста ячеек;
- чередование строк;
- компактный режим;
- добавление и удаление конкретных столбцов;
- быстрое добавление и удаление последней строки;
- новые параметры сохраняются в документе и валидируются;
- старые таблицы совместимы.

SQL не требуется.

Установка:
node install_v53.mjs
npm run build
git add .
git commit -m "v53: improve tables"
git push
