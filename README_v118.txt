OnlineRepetitor v118 — safe rendering performance pass

Изменено:
- снижена стоимость перерисовки объектов доски через CSS containment;
- во время панорамирования отключаются тяжёлые тени/переходы;
- на touch/mobile отключён backdrop blur у плавающих панелей;
- облегчены тени объектов на мобильных GPU;
- логика доски, realtime, сохранение и серверная часть не менялись.

SQL не нужен.
