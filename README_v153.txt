OnlineRepetitor v153

Звонки полностью убраны из пользовательского приложения:
- BoardVideoCallPanel больше не импортируется в App.tsx;
- кнопка звонка и весь WebRTC UI больше не монтируются;
- код звонков не попадает в production JS bundle, потому что импорт отсутствует;
- исходники звонков отдельно сохранены в архив OnlineRepetitor_call_feature_archive_v150.zip.

В патч также включены исправления загрузки/таймаутов v152.

Полные файлы для замены. Ничего вручную редактировать не нужно.
