OnlineRepetitor v101 — UX clarity and tool panel

Изменено:
- единая читаемая системная типографика;
- панель инструментов больше не является длинной колонкой непонятных значков;
- основные инструменты имеют текстовые подписи;
- редкие инструменты спрятаны под «Ещё инструменты»;
- активный инструмент виден сразу;
- пустая доска получила стартовые действия: рисовать, текст, фигура, загрузить фото/PDF;
- нижняя подсказка показывает текущий инструмент вместо набора букв;
- скорректированы отступы панели масштаба, подсказки и миникарты;
- на узких экранах панель автоматически возвращается к компактным иконкам.

SQL не нужен.
TypeScript: OK.

npm run build
git add .
git commit -m "v101: improve board clarity and tool panel"
git push origin main
