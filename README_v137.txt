OnlineRepetitor v137 - финализация видеозвонков на стороне приложения

Сделано:
- тайм-аут входящего вызова: 45 секунд;
- тайм-аут установки исходящего соединения: 35 секунд;
- зависший звонок автоматически освобождает камеру, микрофон и WebRTC;
- понятные ошибки разрешений, отсутствующих устройств и занятой камеры;
- готовый deploy/coturn с Docker Compose и шаблоном turnserver.conf;
- инструкция по связи coturn с Supabase TURN_SHARED_SECRET / TURN_URLS;
- service-worker cache обновлён до v137.

Проверка:
- TypeScript `tsc -b`: успешно;
- полный `npm run build` проходит TypeScript и останавливается в текущем Linux-контейнере
  только на отсутствующем optional native binding Rolldown.

Статус:
Клиентская часть видеозвонков завершена. Для 100% остаётся инфраструктурный шаг:
развернуть coturn на VPS и проверить звонок между двумя разными сетями.
