OnlineRepetitor v134 - фактическая диагностика ICE-маршрута

Сделано:
- после подключения WebRTC читается selected ICE candidate pair через getStats();
- если выбран relay candidate, интерфейс показывает «Через TURN»;
- если relay не используется, интерфейс показывает «Напрямую»;
- наличие TURN в конфигурации больше не выдаётся за доказательство использования TURN;
- состояние маршрута очищается после завершения звонка.

Проверка:
- npx tsc -b: успешно.
- полный npm run build в контейнере блокируется отсутствующим native optional binding Rolldown;
  исходники проходят TypeScript-проверку.

Следующий этап:
- развернуть реальный coturn и Edge Function;
- проверить звонок Wi-Fi <-> мобильный интернет;
- затем завершить QA видеозвонков.
